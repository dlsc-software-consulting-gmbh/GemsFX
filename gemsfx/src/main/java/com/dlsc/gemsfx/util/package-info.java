/**
 * Provides utility classes shared by GemsFX controls and skins.
 * <p>
 * The package includes localization support, JavaFX styling helpers, history
 * management, selection and virtual-flow utilities, HTTP status metadata, and
 * companion stylesheet applicators. These classes are intended to centralize
 * reusable infrastructure code that does not belong to a single control.
 */
package com.dlsc.gemsfx.util;
