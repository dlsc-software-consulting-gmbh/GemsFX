### ChipsViewContainer

A flow pane that displays a list of `ChipView` instances, for example the currently selected
filter values. The container also manages a hyperlink for clearing the entire selection and it
hides itself automatically whenever the list of chips is empty.
