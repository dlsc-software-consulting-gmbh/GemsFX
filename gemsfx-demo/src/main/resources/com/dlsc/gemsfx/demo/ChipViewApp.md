### ChipView

A small "badge-style" view representing a model object. A chip shows a text, an optional
graphic, and a close icon. The close icon only appears when the application has registered a
close handler via `setOnClose(...)`. Inside GemsFX the chips are used for displaying the
currently active filters of a `FilterView`.
