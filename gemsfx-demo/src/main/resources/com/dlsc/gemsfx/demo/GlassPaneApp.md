### GlassPane

A pane that covers the user interface with a semi-transparent color, indicating that the
application is busy and that input is blocked. The pane can fade in and out, and both the
duration of the animation and the opacity used for blocking can be configured.
