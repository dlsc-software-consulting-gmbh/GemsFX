/* SPDX-License-Identifier: MIT */

/**
 * A copy of the "atlantafx-decorations" library by mkpaz
 * (<a href="https://github.com/mkpaz/atlantafx">github.com/mkpaz/atlantafx</a>), which is
 * licensed under the MIT license (see {@code LICENSE.txt} in this package).
 *
 * <p>The library provides the window buttons (close, minimize, maximize) that the showcase
 * shows inside of its {@link javafx.scene.layout.HeaderBar}, together with a set of themes that
 * make the buttons look like the buttons of macOS, Windows, or Gnome. The code was copied into
 * the showcase because the library is currently not available in any public Maven repository.
 * Apart from the package name, the location of the theme stylesheets, the removal of the
 * optional JSpecify annotations, and the use of the "leading" / "trailing" properties of the
 * final {@code HeaderBar} API, the code is unchanged.
 */
package com.dlsc.gemsfx.showcase.decorations;
